#include "../../functional.h"
#include "../../Connection.h"
  
// Weights as const array here, use pointer in configuration struct
float const w_${name}[] = $weights;

// post, pre, w
ConnectionConf const conf_${name} = {$input_size, $output_size, w_${name}};
